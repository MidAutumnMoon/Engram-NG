    <PREVIOUS MESSAGES>
    {{chatHistory}}
    </PREVIOUS MESSAGES>

    <NEW ENTITY>
    {{newEntity}}
    </NEW ENTITY>

    <CANDIDATE EXISTING ENTITIES>
    {{candidates}}
    </CANDIDATE EXISTING ENTITIES>

    判断 NEW ENTITY 是否与 CANDIDATE EXISTING ENTITIES 中的某一个指代同一实体。
    判断依据：名称、别名、身份、在剧情中的定位。名称/别名不同但指同一对象时才算 duplicate。
    只输出 JSON。
